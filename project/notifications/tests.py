from django.test import TestCase

# Create your tests here.

class NotificationAppTests(TestCase):
    def test_basic_addition(self):
        """
        A basic test to ensure the test suite is running.
        More specific tests will be added here once models and views are defined
        for the notifications app.
        """
        self.assertEqual(1 + 1, 2)
